#!/usr/bin/env python
# coding=utf-8
from math import sqrt

for n in range(99, 1, -1):
    root = sqrtdf(n)
    if root == int(root):
        print inf
        continue
    else:
        print "Nothing"
